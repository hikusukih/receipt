<%= form_for(@groupitem) do |f| %>
  <% if @groupitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groupitem.errors.count, "error") %> prohibited this groupitem from being saved:</h2>

      <ul>
      <% @groupitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br>
    <%= f.text_field :group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
