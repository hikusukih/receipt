<p id="notice"><%= notice %></p>

<p>
  <strong>Item:</strong>
  <%= @groupitem.item.name %>
</p>

<p>
  <strong>Group:</strong>
  <%= @groupitem.group.name %>
</p>

<%= link_to 'Edit', edit_groupitem_path(@groupitem) %> |
<%= link_to 'Back', groupitems_path %>
